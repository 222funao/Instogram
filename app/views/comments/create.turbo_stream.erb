<%= turbo_stream.append "comments_#{@post.id}",
      partial: "comments/comment",
      locals: { comment: @comment } %>

<%= turbo_stream.replace "comment_form_#{@post.id}",
      partial: "comments/form",
      locals: { post: @post } %>
